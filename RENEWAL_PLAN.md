# 就活ボード リニューアル計画

機能は今の版と同じまま、中の作りだけを組み直す。データの置き場所は今と同じ Google スプレッドシートと Apps Script（GAS）で、画面は GitHub Pages に置く。

変える柱は4つある。

1. 行ごとに変わらない ID を持たせる
2. 状態を1列にまとめる
3. 状態の変わり方を1つのファイルにだけ書き、画面とGASで同じものを使う
4. カレンダーは「あるべき予定」との差分だけを直す形にする

スプレッドシートを手で編集しないので、シート編集で動くトリガーと独自メニューはすべて外す。

---

## 1. 今の版で起きていた問題と、その原因

| 起きていたこと | 原因 | リニューアルでの手当て |
|---|---|---|
| 見送りにしてもカレンダーの通知が残る | 操作ごとに「この予定を消す」を個別に書いていて、書き忘れた | カレンダーは行の状態から毎回計算し、差分だけ直す（5章） |
| 落選から戻しても落ちた段階が残る | 状態をI列と「状態」列の2か所で持ち、手で合わせていた | 状態は1列だけ |
| 内定にしたのに再読み込みで段階が戻る | 次の段階の計算を画面とGASの両方に書き、食い違った | 計算は `domain.js` の1か所だけ |
| 同時に操作すると別の会社に書き込むおそれ | 行を「会社名＋区分」で探し、書くたびにシートを並べ替えていた | 行は ID で探す。シートは並べ替えない |
| ロゴの記録が増えると保存できなくなる | スクリプトのプロパティ1件（約9KB）に全社分を入れていた | ロゴは会社の行の列に持つ |
| 鍵がコードに書いてある | `WA_KEY` を定数で持っていた | スクリプトのプロパティに移す |

---

## 2. 残す機能の一覧（移し忘れ防止のチェックリスト）

リニューアル版は、ここに並ぶ機能がすべて動くまで旧版と並行して動かす。

印の意味：「画面」は開発用のデータで画面を触って確かめたもの、「テスト」は `npm test` で確かめたもの。
`[ ]` の項目は、括弧の中の「残り」を画面で確かめたら印を付ける。

### 接続と同期
- [x] 接続設定（ウェブアプリURLと鍵を端末にだけ保存）（画面・テスト）
- [x] 接続先を変える（記録タブの下。接続先が変わったら、id で覚えている端末の保存を捨てる）（画面・テスト）
- [x] 起動時は端末の保存分を先に出し、裏で最新を取る。「更新中…」と取得失敗の表示（テスト）
- [ ] 引っ張って更新（テスト済み。残り：スマホで引っ張って「最新に更新しました」が出る）
- [x] 保存を待たずに画面を先に変え、失敗したら元に戻して理由を出す（画面・テスト）
- [x] 入力中は裏の再取得で描き直さない。描き直しても書きかけの値とカーソル位置を残す（テスト）
- [x] ルート編集は 400ms まとめて送り、保存待ちの分を取得データの上に重ねる（テスト）
- [x] 画面を離れるときに、保存待ちの編集を送り切る（テスト）
- [ ] オフラインで開ける（sw.js）（保存の決まりはテスト済み。残り：実機で機内モードにして開く）

### 一覧画面
- [x] 区分の切り替え（夏インターン／本選考）と各区分の件数（画面・テスト）
- [x] 管理用の行（テストセンターなど）を上部の帯に出す（テスト）
- [x] 見出し：次の締切か予定を秒単位で数える。開催中の表示、期限切れで一覧を組み直す。見出しに出ていない締切や予定の時刻を過ぎたときも組み直す（画面・テスト）
- [x] 絞り込み（すべて／予定／対応中／内定／参加決定／結果待ち／終了／見送り）と件数。選択を保存し、対象が0件なら「すべて」に戻す（テスト）
- [x] レーン：予定、対応中（締切の近い順）、内定、参加決定、結果待ち（提出日順）、終了と見送り（折りたたみ）（画面・テスト）
- [x] タイル：ロゴ、短くした社名（法人格と全角英数を落とす）、段階、日付、右端の日数表示（TODAY／経過日数／開催中／参加／内定／終了／見送り／未定）（画面・テスト）
- [x] 締切まで2日以内は赤く、終了・見送りは薄く（テスト）
- [x] 締切を過ぎた対応中の行は、自動で結果待ちとして扱う（「締切経過で自動送り」）（テスト）
- [x] 表示中のページ・区分・絞り込みを保存（テスト）

### 詳細
- [ ] スマホはシート（下へスワイプで閉じる、背景が縮む、戻る操作で閉じる）、幅1000px以上は右パネル（戻る操作と右パネルはテスト済み、スマホの操作は画面で確認。残り：下へスワイプで閉じる）
- [x] タブ：概要／予定（件数つき）／選考ルート（テスト）
- [x] 概要：締切までのカウントダウンと締切の編集（日付＋時・分のプルダウン、分は 00/30/59）（テスト）
- [ ] 概要：結果待ちの経過日数と「通過／落選」、参加決定なら実施日まで（通過・落選はテスト済み。残り：参加決定の会社の「実施日まで」）
- [ ] マイページを開く（Chromeで開く設定つき）、書類フォルダを開く、ログインIDをタップでコピー、結果日（リンクの行き先とコピーはテスト済み。残り：実機で Chrome が開く、結果日の表示）
- [x] パスワードをタップでコピー（押したときだけ取りに行く。新しく足す機能）（画面・テスト）
- [x] 完了にして結果待ちへ、対応中に戻す、参加決定にする、1段階戻す、見送り、本選考に引き継ぐ（画面・テスト）
- [ ] Googleカレンダーに追加するリンク（残り：押して追加画面が開く）
- [ ] 設定（折りたたみ、開閉を覚える）：マイページURL・ID、業種（候補つき）、会社名の変更、同じマイページで別の選考を追加、ロゴ（URL指定／自動で取り直す／画像ファイルを96pxに縮めて端末に保存）、削除（2回確認）（ロゴの「自動で取り直す」と「画像ファイル」以外はテスト済み。残り：その2つ）
- [x] 予定タブ：残り日数つきの一覧、削除、追加（種別、終日、開始・終了、連日、場所）（テスト）
- [x] ルートタブ：進み具合の線表示、段階名タップで現在地、並べ替え、削除（2段階以上を残す）、候補から追加、自分で名前を付けて追加（次から候補にも出る）（テスト）
- [ ] 現在の段階も削除できる。消したら次の段階が現在地（最後なら1つ前）。消す前に「〇〇を消して、現在地を△△にします」と確かめる。締切は残す。落選した会社の落ちた段階も同じ（新しく足す機能）（テスト済み。残り：開発用で1社消してみる）
- [ ] 「次とまとめて結果が出る」の印：ルートタブで段階ごとに付け外し（最後の段階には付けられない）。印の段階では「完了にして結果待ちへ」の代わりに「提出して〇〇へ」を出し、結果待ちを通らず次の段階の対応中にする（締切と締切の予定は消える）。1段階戻すで、つながりの途中に戻れる（新しく足す機能）（テスト済み。残り：開発用で1社に印を付けて、提出して次へ進む）
- [ ] 初期のルート：夏インターンは ES → テスト → 面接 → インターン、本選考は ES → テスト → 面接 → 内定。新しい会社の最初の段階は ES。ルートを編集していない会社（route が空）は前の初期のルートを書き込んで固定する（`migrate` と `fillEmptyRoutes`）（新しく足す機能）（テスト済み。残り：開発用で `fillEmptyRoutes` を実行し、会社を1つ足して ES から始まる）

### 追加と検索
- [ ] 会社名を2文字入れると Wikidata から候補。選ぶと正式名称とロゴ用ドメインが入る（残り：画面で候補が出て、選ぶとドメインが入る）
- [x] 追加時にURL・ID・ドメイン・段階・締切を入れられる。同じ区分の重複は弾く（テスト）
- [x] 検索（Cmd/Ctrl+K、Enterで先頭を開く）。両区分から探す（テスト）

### 記録タブ
- [x] エントリー社数、通過した選考の数、進行中・終了・見送りの数、最も進んだ段階（ルート内の割合）（テスト）
- [x] 一番多く終わった段階（テスト）
- [x] 段階別の通過率（テスト）
- [x] 結果待ちの件数・平均経過日数・最長（テスト）
- [x] 業界別の進み方（決着した社のうち決定まで行った割合、平均到達）（テスト）
- [x] 業界×段階の通過率（テスト）
- [x] 進行中の内訳（テスト）
- [x] 業種が空欄なら社名のキーワードから推定する（テスト）
- [ ] 印でつながった段階は1つの関門として数える（「ES＋テスト」で1行）。通過・落選はつながりの最後で1回だけ。途中の段階で落選しても、その関門の落選（新しく足す機能）（テスト済み。残り：開発用で印を付けた会社が記録タブで1行になる）

### ロゴ
- [x] 手動指定を最優先（URL指定はサーバーにも保存、画像ファイルは端末だけ）（テスト）
- [ ] 自動取得の順：Wikidata の公式ロゴ → DuckDuckGo で有無を確かめて Google の256px版 → サイト直下の favicon.ico → Wikipedia の画像（残り：ロゴの無い会社で自動で出る）
- [x] 採用管理システムのドメインはロゴ探しに使わない（テスト）
- [ ] 同時に3件までの順番待ち（残り：旧版と同じ作りを移しただけで、テストは無い。画面でロゴが順に出れば足りる）
- [x] 表示に失敗したら次の候補へ差し替える（同じURLは繰り返さない）（テスト）
- [ ] 一度表示できたURLを覚えて、描き直しのちらつきを防ぐ（残り：保存したときにロゴがちらつかない）
- [x] 旧版のロゴの記録を取り込む（`importLegacyLogos`。写真と https でない値は外す）（画面・テスト）
- [x] 旧版の画面で手で入れたロゴ（端末の `sk_manual_logos`）を、新版を初めて開いたときに一度だけ写す（新しく足す機能）（テスト）

### GAS側
- [x] 鍵による認証（画面・テスト）
- [x] 締切の予定（時刻ありは30分枠、なしは終日。通知は4日前と2日前）（画面・テスト）
- [x] 予定の登録（単発・終日・連日。連日は日ごとに別の予定、最大60日。通知は前日と1時間前）（テスト）
- [ ] 会社ごとの Drive フォルダ作成とリンク。分割・引き継ぎでは同じフォルダを指す（模擬環境ではテスト済み。残り：本物の Drive にフォルダができる）
- [ ] 削除時は予定とカレンダーもまとめて消し、Drive のフォルダは残す（模擬環境ではテスト済み。残り：本物のカレンダーから消え、フォルダは残る）

### 外すもの（手でシートを編集しないため）
- シート編集で動く `syncToCalendar` とその登録
- 開いたときの独自メニューと並べ替え、罫線
- I列の「削除」「日程削除」「選考落ち」の入力コマンド
- 旧状況列（I列）と、その合わせ込み
- 一度きりの移行用関数（状態列の変換、区分の埋め込み、ドメインの自動入力など）
- 結果発表の予定日（N列）と、その予定をカレンダーに作る処理
- Gmail からの締切取り込み（`fetchDeadlinesFromGmail`）と、そのためのラベル「就活シート連携済」の作成。Gmail の権限もプロジェクトから外す

---

## 3. 判断が要る点（作り始める前に決める）

決めたこと

- Gmail からの締切取り込みはやめる（2章「外すもの」に移した）。
- パスワードは残す。ただし扱いを変える（下の「パスワードの扱い」）。
- 結果発表の予定日（N列）は機能ごと外す（2章「外すもの」に移した）。今ある結果発表の予定は消さずに残し、説明欄のパスワードの行だけを消す。
- 見送り・落選にしたら、これから先の予定はカレンダーから消す。終わった予定は残す。対応中に戻せば、先の予定は作り直す。

まだ決めていないこと

1. **区分**：夏インターンと本選考の2つで固定のままでよいか。秋冬インターンを足す見込みがあるなら、区分を一覧で持てるようにしておく。
2. **管理用の行**：今は「テストセンター」「Webテスト」という会社名で見分けている。種類を表す列を持たせ、名前に頼らないようにするのを勧める。

### パスワードの扱い

覚えていないものがあるので残す。今は「シートにはあるが画面では見られず、カレンダーの説明欄には毎回書き込まれる」状態で、見たいときに見られない一方、漏れやすい場所には出ている。これを逆にする。

- シートの `pw` 列にだけ持つ。カレンダーの説明欄には書かない（移行時に、今ある予定の説明欄からも消す）
- `getData()` の応答には含めない。一覧の読み込み、25秒キャッシュ、端末の保存分のどこにも載らない
- 詳細の「パスワードをコピー」を押したときだけ、`getPassword(id)` で1社分を取りに行き、そのまま控え帳（クリップボード）へ入れる。画面には `••••••` とだけ出し、変数にも残さない
- 設定の「マイページの登録」で入力・変更できる。入力欄は伏せ字にする
- `getPassword` は鍵に加えて、呼んだ時刻を実行ログに残す（中身は残さない）

---

## 4. データの持ち方

シートは `companies` と `events` の2枚にする。どちらも1行目が見出しで、見出しの名前で列を探す（列の順番に頼らない）。

### companies

| 列 | 中身 | 例 |
|---|---|---|
| id | 変わらない ID（作成時に付ける） | `c_7f3a91` |
| kind | `company` か `mgmt`（管理用の行） | `company` |
| name | 会社名（正式名称） | `株式会社NTTデータ` |
| term | 区分 | `本選考` |
| status | `todo` / `waiting` / `offer` / `joined` / `failed` / `skipped` の1つだけ | `waiting` |
| stage | 現在の段階 | `一次面接` |
| route | 段階の並び（JSON配列）。空なら区分の初期のルート（ふだんは空にしない） | `["ES","テスト","面接","内定"]` |
| routeLinks | 「次とまとめて結果が出る」段階の名前（JSON配列）。最後の段階には付けない | `["ES"]` |
| lostStage | 落ちた段階 | `面接` |
| dueAt | 締切（ISO形式、日本時間） | `2026-10-03T23:59` |
| dueHasTime | 時刻まで決まっているか | `TRUE` |
| submittedAt | 結果待ちに入った日 | `2026-09-20` |
| resultAt | 結果日（内定・参加決定・落選） | `2026-09-28` |
| url / loginId | マイページ | |
| pw | パスワード。`getPassword` からしか読まない | |
| domain / industry | ロゴ用ドメイン、業種 | |
| logo | ロゴのURL（自動・手動とも）。`none` は見つからなかった印 | |
| logoManual | 手動で指定したか | `FALSE` |
| folderUrl | Drive のフォルダ | |
| cal | この会社のカレンダー予定の対応表（JSON）。締切も面接などの予定もここにまとめる。キーは `due`、`ev:予定のID`（連日は `ev:予定のID#日目`）。`sig` は中身の指紋で、同じならカレンダーに問い合わせない | `{"due":{"id":"abc@google.com","sig":"…"}}` |
| updatedAt | 最後に書き換えた時刻（同時編集の検出に使う） | |

「締切を過ぎた対応中」は保存しない。`status` が `todo` のまま、画面側で結果待ちとして見せる。

### events

| 列 | 中身 |
|---|---|
| id | 予定の ID |
| companyId | companies の id（会社名ではなく ID でつなぐ） |
| kind | 面接・説明会・GD・面談・インターン・適性検査 |
| startAt / endAt | 開始・終了（ISO形式） |
| allDay / daily | 終日か、連日か |
| place | 場所・URL |

予定のカレンダー ID は、会社の `cal` 列にまとめて持つ。予定の行を消せば、次に会社を合わせたときに差分でカレンダーからも消える。

どちらのシートも、値は書式なしテキストで置く（`2026-10-03` のような値が日付に化けないように）。

本番シートと開発用シートのタイムゾーンは、アメリカ西海岸（Los Angeles）になっている。日時はいつも日本時間の文字列で持ち、シートから Date を読むときも日本時間に直す（シートのタイムゾーンでは読まない。夏は16時間、冬は17時間ずれる）。

---

## 5. 処理の組み方

### domain.js：状態の変わり方を書く唯一の場所

画面とGASの両方で同じファイルを読み込む。中身は、DOM にもシートにも触らない、入力から出力を返すだけの関数に限る。

- `apply(company, op, now)`：`op` は `{ type: 'setDue', dueAt: '…' }` の形。`pass`（通過）、`fail`、`done`（結果待ちへ）、`advance`（印の段階を出して次の段階へ）、`reopen`（対応中に戻す）、`skip`、`join`（参加決定にする）、`prev`、`setDue`、`setRoute`、`setLink`（まとめの印）、`removeStage`（段階を消す。今の段階も消せる）、`setInfo`、`setIndustry`、`setDomain`、`setLogo`、`rename` を受け取り、新しい会社データを返す。元のデータは書き換えない。できない操作は、画面にそのまま出せる文言で Error を投げる
- `defaultRoute(term)`、`LEGACY_ROUTE`、`linksOf`、`isLinked`、`gatesOf`（印でつながった段階をまとめた関門の並び）、`nextStage`、`removalTarget`（段階を消したときの新しい現在地）：ルートの読み方
- `create(id, fields)`、`carryOver(src, id)`、`split(src, id, newName)`、`duplicateOf(companies, name, term, exceptId)`：会社を作る処理と重複の確認。ID は呼び出し側で作って渡す
- `createEvent(id, companyId, fields)`：予定タブの入力をそろえる
- `viewStatus(company, now)`：締切切れの自動送りを含めた、画面上の状態
- `desiredCalendar(company, events, now)`：その会社に本来あるべきカレンダー予定の一覧。締切の予定は `status` が `todo` のときだけ置く。面接などの予定は、見送り・落選のときは終わった分だけを残す
- `calendarDiff(desired, current)`：あるべき予定と作ってある予定を照らし、作る・直す・消す・そのまま、に分ける
- `tally(companies, now)`：記録タブの集計（表示中の区分の分だけを渡す）
- `industryOf(name)`、`industryFor(company)`、`shortName(name)`：業種推定と社名の短縮

日時は日本時間の文字列（`2026-10-03T23:59`）で持ち、計算も日本時間で行う。端末や GAS のタイムゾーン設定に左右されない。最後まで通ったときの状態は、区分名に「インターン」が入っていれば `joined`、それ以外は `offer`。

画面は操作時に `apply` で先に表示を変え、GASも同じ `apply` で保存する。計算が1つなので食い違わない。

GASはESモジュールを読めない。そこで `domain.js` は、グローバルに `Domain` を1つ置く書き方にする。`npm run sync` で `gas/` フォルダへコピーし、`clasp push` で送る。

### カレンダーは差分で合わせる

書き込みのたびに次を行う。

1. `desiredCalendar` で、あるべき予定を出す（キー・見出し・日時・通知）
2. 行の `cal` 列にある、実際に作った予定の ID と照らす
3. 無いものは作り、内容が違うものは直し、要らないものは消す

見送り・落選・削除・改名のどれでも同じ手順で済み、個別の後片付けを書かない。

### GAS の API

呼び出しは POST だけで受ける（鍵が URL に残らないように）。本文は `{ key, action, args }` で、`args` はオブジェクト。

- `getData()`：全件を返す（25秒の読み取りキャッシュつき）。パスワードとカレンダーの対応表は載せない
- `mutate({ id, op, args, updatedAt })`：1社への操作。返すのは、その会社と予定だけ
- `addCompany`、`carryOver({ id })`、`splitCompany({ id, name })`、`deleteCompany({ id })`、`addEvent`、`deleteEvent({ id })`
- `getPassword({ id })`：1社分のパスワードだけを返す。キャッシュしない。ロックも取らない
- `setPassword({ id, pw })`：パスワードを変える。`updatedAt` は変えない
- エディタから実行するもの：`checkSetup`、`migrate`、`syncAllCalendars`、`inspectCalendar`、`fillEmptyRoutes`（ルートが空の会社に前の初期のルートを書き込む）、`undoMigration`、`importLegacyLogos`（旧版のロゴの記録を取り込む。手順は `v2/GAS_SETUP.md` の 6-4）
- `saveLogo({ id, logo, manual })`：ロゴの2列だけを書く。`updatedAt` もカレンダーも変えない（画面は開くたびに自動でロゴを探して保存するので、`updatedAt` を変えると同じ端末の操作とぶつかる）

`mutate` は `updatedAt` が保存済みの値と違えば書かずに `conflict` を返し、最新の会社を添える。スマホとPCで同時に触ったときに、後から来た古い操作で上書きしないためである。ロックはルーターで1回だけ取る。

シート・カレンダー・Drive の親フォルダ・鍵は、スクリプトのプロパティから読む。カレンダーは必ず ID から開き、既定のカレンダーは使わない。準備と移行の手順は `v2/GAS_SETUP.md` にある。

### 画面のファイル分け

```
index.html          骨組みだけ
css/app.css         旧版の見た目をそのまま移したもの
js/boot.js          ページから読む入口。start() を呼ぶだけ
js/main.js          起動と画面の切り替え。クリックなどを1か所で受け、data-act の名前で処理を選ぶ
js/api.js           通信
js/store.js         データの保持、先に表示を変える処理、失敗時の巻き戻し、保存待ちの管理
js/html.js          自動でエスケープするテンプレート関数（esc の付け忘れを無くす）
js/state.js         表示中のページ・区分・絞り込みなど
js/storage.js       端末の保存領域（キーは必ず sk2_ で始める）
js/format.js        日時の見せ方と予定の読み方
js/views/list.js / detail.js / passport.js / add.js / search.js / setup.js / fields.js
js/ui/sheet.js      シートの開閉とスワイプ
js/ui/liquid.js     下のタブバーと区分切り替えのつまみ
js/ui/notice.js     トースト・「更新中…」・保存中の覆い・控え帳へのコピー
js/logo.js
shared/domain.js
sw.js / manifest.json
```

ビルド作業の要らないESモジュールで書き、GitHub Pages にそのまま置く。`window._xxx` に置いていたタイマーや状態は、それぞれのモジュールの中に閉じ込める。

- HTML に `onclick` は書かない。ボタンには `data-act` を付け、`main.js` が名前で処理を選ぶ
- `store.js` は会社ごとに「サーバーで確定した写し」と「未確定の操作の列」を持ち、画面には確定した写しに未確定の操作を `Domain.apply` でかけたものを出す。失敗した操作だけを列から外せば元に戻り、裏で最新が届いても未確定の操作は消えない。操作は会社ごとに1つずつ送る。ルートの編集は 400ms 待って1つにまとめる
- 旧版と新版は同じオリジンに置くので、端末の保存領域もキャッシュも共有になる。新版の保存領域のキーは `sk2_`、`sw.js` のキャッシュ名は `shukatsu2-` で始め、旧版の分には触らない
- `sw.js` は、画面のコードをネット優先で取る（ファイルが分かれているので、古い保存版を先に返すと新旧が混ざる）。ロゴと部品は保存版を先に返す

---

## 6. テスト

`v2/` で `npm test` を回すと全部通るようにする。

- `domain.test.js`：状態の変わり方、締切切れの扱い、あるべきカレンダー予定、集計（Node 標準の `node:test`）
- `gas.test.js`：シート・カレンダー・ロックの模擬環境（`test/gas_mock.js`）で API と移行を通す。模擬環境では既定のカレンダーやユーザーのプロパティを呼ぶとエラーになり、並べ替えもできない
- `ui.test.js`：jsdom の上で本物の画面のコードを起動し、裏は模擬環境の GAS につないで、起動・一覧・詳細・主な操作を通す
- `sw.test.js`：オフラインとロゴの保存。保存するコードの一覧が実際のファイルとそろっているか、旧版のキャッシュを消さないかも確かめる

手元で画面を目で確かめるときは `node scripts/dev-server.js`（`v2/GAS_SETUP.md` の8）。

2章のチェックリストのうち、画面で確かめられるものは `ui.test.js` にも1項目ずつ置く。

---

## 7. 進め方

順番に意味があるので、この順で進める。

1. **準備**：新しいリポジトリ（または `v2/` フォルダ）を作る。本番シートのコピーを作り、開発中はそちらだけを使う。
2. **domain.js とテスト**：画面もGASもまだ作らず、状態の変わり方だけを先に固める。
3. **GAS**：API と、コピーしたシートを新しい形に移す `migrate()` を作る。移行では、旧I列と「状態」列から `status` を決め、日付・時・分を `dueAt` にまとめ、フォルダの数式からURLを取り出し、ロゴをプロパティから列へ移す。予定は会社名＋区分から `companyId` を引く。D列のパスワードは `pw` 列へ移し、今あるカレンダー予定の説明欄からパスワードの行を消す。今あるカレンダー予定のID（K列、予定シート）は `cal` 列に引き継ぎ、作り直さない。O列の結果発表の予定は引き継がず、説明欄のパスワードの行だけを消す。予定の ID を引き継ぐのは本番で移すときだけで、開発用シートで試すときは引き継がずに開発用カレンダーへ作り直す（開発用シートの中の ID は本番のカレンダーを指しているため）。
4. **画面**：今の見た目をそのまま移し、中身を5章の形に分ける。
5. **並行運用と切り替え**：新版を `/v2/` で動かし、2章のチェックリストが全部埋まったら、本番シートで `migrate()` を実行して切り替える。旧版の GAS デプロイは1か月ほど残しておく。当日の作業順・旧版を触らない時間・戻し方は `v2/GAS_SETUP.md` の「本番に切り替える」にある。


---

## 8. 今どこまで済んでいるか（2026-09-26 時点。新しいセッションはここから読む）

### 済んだこと

- 手順1〜4（準備・domain.js・GAS・画面）は済んだ。新版の画面は github.io の `/shukatsu/v2/` に出ている。テストは `v2/` の `npm test` で207件すべて通る
- 開発用：単独の GAS プロジェクト「就活ボード v2（開発）」。スクリプトのプロパティ（シート・カレンダー・Drive の親フォルダ・鍵・`LEGACY_LOGOS`）は入れてある。ウェブアプリ「v2 開発版」は公開済みで、今は版3（選考ルートの印などが入った版）
- 開発用シートで移行を一度通した：`migrate`、`syncAllCalendars`、`importLegacyLogos`（53社に入った）、パスワードの一括入力（14社）。そのあと日時が16〜17時間ずれていたのを直した（シートのタイムゾーンが Los Angeles のため。日時はいつも日本時間で読む）
- 開発用の移し直し（6-6）は済んだ。`migrate`：会社56件・予定5件。`importLegacyLogos`：53行はもう同じ（`migrate` で入っていた）。`syncAllCalendars`：56社を確かめ15社の予定を直して「すべて終わりました」。旧版で 9/24 18:30〜19:30 の説明会は、新版でも同じ時刻に出る
- 本番用：単独の GAS プロジェクト「就活ボード v2」を作り、最新のコードを送ってある（`npm run push:prod`）。プロパティはまだ入っていないので、どこにもつながっていない
- 切り替えの段取り・旧版を触らない時間・戻し方は `v2/GAS_SETUP.md` の「本番に切り替える」に書いてある。当日に一番上の `index.html` と差し替えるページは `v2/cutover/root-index.html` に用意してある（一番上の `index.html` は当日まで変えない）
- 2章のチェックリストに、画面で確かめたものとテストで確かめたものの印を付けてある

### 報告待ちで止まっていること

1. **パスワードの一括入力**（6-5）。移し直したので、前に入れた14社分は消えている。パスワードの文字列はファイルに書かないので、新しいセッションでは本人にもう一度聞く。一時ファイル `v2/gas/fill-password.local.js` を作って `npm run push` → 本人が `fillEmptyPasswords` を実行 → すぐ一時ファイルを消して `npm run push:clean` → `clasp clone` で GAS から消えたことを確かめる
2. **本番用のプロパティと `checkSetup`**（「本番に切り替える」の前日の準備 3・4）。手順は伝えたが、結果の報告をまだ受けていない。`ALLOW_PRODUCTION` を入れていないので、「本番のシートを指しています」で止まれば正しい

### 切り替えまでに残っていること

- 2章のチェックリストの `[ ]` の項目（17件）。どれも「残り」に、画面で何をすれば確かめられるかを書いてある
- 選考ルートの新しい機能（まとめの印・今の段階の削除・初期のルート）を開発用で触って確かめる（チェックリストにある）
- 前日の準備の 5（本番用のウェブアプリを公開する）と 6（旧版の GAS の時間で動くトリガーを止める）
- 当日の作業（「本番に切り替える」の表の1〜14）
- GAS のコードを直したら、`npm run push` で開発用に送り、開発用のウェブアプリを同じデプロイ ID のまま新しい版に差し替える（`npx clasp create-deployment --deploymentId <ID> --description "v2 開発版"`）。本番用へは `npm run push:prod`（本番に関わるので、送る前に本人に聞く）
- 3章の「区分」はまだ決めていない（今は夏インターンと本選考の2つで固定。`v2/js/state.js` の `TERMS`）。「管理用の行」は、`kind` 列で見分ける形で作ってある

### このセッションで決めたことのうち、ほかのファイルに書いていないもの

- **push の決まり**：テストが全部通ったら、commit と push まで進めてよい。次のときは push の前に本人に聞く：本番のシート・カレンダー・GAS に関わる変更／旧版の画面（一番上の `index.html`・`sw.js`・`manifest.json`）を変えるとき／パスワードや鍵が混ざっていないか少しでも怪しいとき。push でログインを求められたら止まって伝える
- **push の前に確かめること**：commit の中身を検索して、パスワード・デプロイ ID・スクリプト ID・シートやカレンダーや Drive の ID が入っていないことを見る
- **git**：この PC では `git` にパスが通っていない。GitHub Desktop に付いている `C:\Users\tsuch\AppData\Local\GitHubDesktop\app-*\resources\app\git\cmd\git.exe` を使う。push のログインは通るようになっている
- **ID を置く場所**：スクリプト ID は `v2/.clasp.dev.json`・`v2/.clasp.prod.json`、デプロイ ID は `npx clasp list-deployments` で見る。どちらもリポジトリ（公開）には書かない。`v2/.clasp.json` はいつも開発用にしておく
- **手元で画面を試すサーバー**：本人は 8787 番（`node scripts/dev-server.js`）を使うことがあるので、Claude が試すときは `.claude/launch.json` の `v2-dev-8788`（8788番）を使う。中身は作り物のデータで、Google にはつながない
- **スマホの使い方**：iPhone も Chrome で使っていて、旧版はホーム画面のアイコンから全画面で開いている。アイコンは保存領域が別なので、切り替えの日はアイコンを置き直さず、一番上の `index.html` を `/v2/` へ移るページに差し替えて、同じアイコンの中で新版を開く（段取りは GAS_SETUP.md に書いてある）
- **旧版のファイル**：`legacy-gas/` の `WebAppApi.gs` は鍵を空にしてある。本番の旧版の GAS に貼ってはいけない。本番の旧版の GAS は、すでに直した版（`waRouteOf_` がある版）になっている
- **legacy-logos.json**：リポジトリの一番上に置いてあるが `.gitignore` に入れてあり、commit しない
