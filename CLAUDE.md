# 就活ボード

就活の選考状況を管理する個人用アプリ。画面は GitHub Pages、データは Google スプレッドシート、処理は Google Apps Script（GAS）。使うのは本人だけで、スプレッドシートを手で編集することはない。

リニューアルの方針と、残す機能の一覧は `RENEWAL_PLAN.md` にある。作業を始める前に必ず読むこと。

## 守ること

- 状態の変わり方（通過・落選・見送りなど）は `shared/domain.js` にだけ書く。画面や GAS に同じ計算を書かない。
- `domain.js` は DOM にもシートにも触らない。入力を受けて結果を返すだけにする。
- 行は `id` で探す。会社名で探さない。シートの行を並べ替えない。
- カレンダーは `desiredCalendar` との差分で直す。操作ごとに個別の削除処理を書かない。
- HTML は `js/html.js` のテンプレート関数で組み立てる。文字列をそのまま `innerHTML` に入れない。
- 鍵などの秘密はコードに書かない。GAS はスクリプトのプロパティ、画面は端末の保存領域に置く。
- パスワードはシートの `pw` 列にだけ置く。カレンダー・ログ・`getData` の応答・キャッシュ・端末の保存領域には載せない。読むのは `getPassword(id)` だけ。
- `sw.js` の保存方針を変えたら `VERSION` を上げる。

## 作業の流れ

- 変更したら `npm test` を回し、全部通ってから終える。
- 機能を足したり変えたりしたら、`RENEWAL_PLAN.md` の機能一覧とテストも合わせて直す。
- GAS の反映は `npm run sync` → `clasp push` → ウェブアプリの再デプロイ（新しいバージョン）。
- 本番シートには触らない。開発中はコピーしたシートを使う。

## 書き方

- 私への返事・報告・途中経過は日本語で書く。
- コメントとメッセージは日本語。硬い言葉を避け、同級生に話すときの言葉で書く（例：「担保する」→「確保する」、「設計する」→「組み立てる」）。
- コメントには「何をしているか」より「なぜそうしているか」を書く。
- 画面の文言は短く、句点で終える。
- 過去の版の事情を書いたコメント（「以前の版では〜」）は残さない。必要なら commit メッセージに書く。
